return {
    s({ trig = ":.", snippetType = "autosnippet" }, t("because"), { condition = in_mathzone }),
    s({ trig = ".:", snippetType = "autosnippet" }, t("therefore"), { condition = in_mathzone }),
}
